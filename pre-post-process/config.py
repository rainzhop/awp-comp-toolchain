from inputs import EQ_ID

# crust1.0目录
CRUST1_0_DIR = "./crust1.0/"

# 前处理输出目录
PRE_OUTPUT_DIR = "../results/" + EQ_ID + "/mesh_source/"

# 后处理+可视化输入目录
path_input = "../results/" + EQ_ID + "/output_sfc/"
input_filename_prefix = 'SZ000'
input_filename_suffix = ['1000', '2000']
path_tmp = "../results/" + EQ_ID + "/tmp/"

# 后处理+可视化输出目录
path_output = "../results/" + EQ_ID + "/output_pics/"
output_filename_prefix = 'awp'
